<p id="comment_<%= comment.id %>">
	<b><%= comment.commenter %> said <%= time_ago(comment.created_at) %></b>:<br />
	<%= comment.body %> 
	<%= link_to 'Delete', [comment.post, comment], 
				:confirm => 'Are you sure?', 
				:method => :delete %>
</p>